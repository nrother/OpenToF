__pycache__

# IDE/tooling artifacts that can land at the repo root if it's opened as a single project
# (Flutter/Android-specific ignores of the same kind live in app/.gitignore).
.idea/
*.iml
.dart_tool/

# Third-party reference model, not redistributed; download it yourself (see cad/README.md).
cad/Veriflite.stl
